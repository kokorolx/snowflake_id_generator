# frozen_string_literal: true

# Specify your gem's dependencies in snowflake_id_generator.gemspec
source 'https://rubygems.org'
group :development, :test do
  # Add gem dependencies used during development and testing
  gem 'rspec'
end
